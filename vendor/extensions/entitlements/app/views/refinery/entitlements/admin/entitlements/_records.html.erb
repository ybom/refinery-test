<%= render 'refinery/admin/search_header', :url => refinery.entitlements_admin_entitlements_path %>
<div class='pagination_container'>
  <% if @entitlements.any? %>
    <%= render 'entitlements' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
