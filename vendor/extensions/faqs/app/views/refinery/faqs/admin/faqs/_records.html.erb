<%= render 'refinery/admin/search_header', :url => refinery.faqs_admin_faqs_path %>
<div class='pagination_container'>
  <% if @faqs.any? %>
    <%= render 'faqs' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
